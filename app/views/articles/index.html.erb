<h1>List of Articles</h1>
<ul>
  <% @articles.each do |ar| %>
    <% unless ar.archived? %>
      <li>
        <%= link_to(ar.title, ar) %>
      </li>
    <% end %>
  <% end %>
</ul>

<%= link_to("New Article", new_article_path) %>

